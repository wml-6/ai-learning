# AI 学习路线规划

> 定位：个人学习、尽可能降低成本、无独立 GPU（CPU 为主）的路线图。
> API：DeepSeek。所有产出存放于 `~/ai-learning/`。
> 本文件初版生成于 2026-09-08，基于当时对环境的实际勘察。
> 学习方法总原则见第 8 节「方法论」。核心一句话：**不一次性给全答案，把实践拆成可执行步骤，每步讲清"做什么 / 为什么 / 怎么做 / 怎么验证"，先跑通再深究。**

> **决策记录（2026-09-08）**：TBD-1 已定——阶段1 五个方向全做；执行顺序见第 4.4 节。TBD-2 已定——DeepSeek key 已有，进入环境变量配置步骤。

---

## 0. 路线总览

最终目标：**能自己训练或微调一个 AI 模型**，并具备理解 AI 原理、在生产中使用 AI 工具的能力。

```
阶段0  环境与工具基线补齐（与阶段1并行，约几天）
   │
阶段1  AI 工具的生产实践（方向由你选，见 TBD-1）      ── 建立"用"的体感
   │
阶段2  机器学习基础（numpy/sklearn + 从0实现算法）   ── 建立"原理"体感
   │
阶段3  深度学习（PyTorch + 从0实现，打通 LLM 原理）  ── 建立"训练"体感
   │
阶段4  训练 / 微调你自己的模型（最终产出）            ── 产出线 A/B/C，见第7节
```

四个阶段彼此不割裂：阶段1 遇到的"黑盒困惑"会成为阶段2/3 学习的问题清单；阶段2/3 掌握的直觉反过来提升阶段1 中你使用工具的效率。

---

## 1. 你的现状基线（2026-09-08 实地勘察结果）

| 项目 | 现状 | 补充说明 / 需要处理的点 |
|---|---|---|
| WSL2 | Ubuntu 26.04 LTS | 版本 2，运行正常 |
| CPU | AMD Ryzen 7 4800H，16 线程 | 纯 CPU 训练的主力，够跑小模型 |
| 内存 | ~7.7 GiB（WSL 分配上限） | 偏小；规划模型和 batch 时要克制 |
| GPU | 无 NVIDIA GPU（核显，无 CUDA / nvidia-smi） | 中大型训练需借助云端免费 GPU，见 TBD-3 |
| 磁盘 | 根分区 ~953 GiB 可用 | 非常充裕 |
| conda | miniconda3，conda 26.7.1 | 位于 `~/miniconda3`，.bashrc 已初始化 |
| Python | base = 3.14.7 | **过新**：torch 等框架对 3.14 支持滞后，需另建专用环境（建议 3.12），见第 2.2 节 |
| conda 环境 | 已有 `myenv` 但为空壳（无 python） | 建议不要用它，按 2.2 新建标准环境 |
| git | 2.53.0；全局 user.name / user.email 已配 | 未生成 SSH key（推 GitHub 需要），见阶段0 |
| VSCode | `~/.vscode-server` 尚未出现 | 首次用 Remote-WSL 连接时自动安装，无需手动处理 |
| DeepSeek API | 环境中无 key | 需你申请并填入，见 TBD-2 |
| 网络 | 未验证到 Google Colab / HuggingFace 等外网连通性 | 影响 TBD-3 的决定 |

---

## 2. 开始前的知识储备（阶段0）

### 2.1 概念上的图景（先定性，后定量）

开始动手前，先用通俗材料建立一张"地图"，无需记公式：

- **AI / ML / DL / 生成式 AI / LLM** 这几个词的关系与边界；
- 一套最小闭环：**数据 → 模型（一堆可调参数）→ 预测 → 与真实结果比较得到误差 → 按误差调整参数（训练）→ 再预测**；
- "训练（training）"与"推理（inference）"的区别；"权重/参数"、"损失函数"、"梯度下降"各自扮演什么角色（只需要直觉）；
- LLM 为什么叫"语言模型"：本质是在学"给定上文，下一个词的概率分布"，即你说的"统计学本质"落到具体形态。

> 这一节我会在阶段0 用 1~2 个比喻 + 一个 20 行的最小 python 例子带你建立。先有定性画面，再进数学。

### 2.2 工具与工作流（环境类补充，含理由）

1. **conda 环境隔离——为什么**
   Python 项目依赖极易互相冲突（如 A 项目要 numpy 1.x，B 要 2.x）。用独立环境隔离，互不污染。这是 ML 项目的基本卫生习惯。

   计划：新建一个 `py312` 环境（`python=3.12`）。
   理由：base 是 3.14，太新，PyTorch / 许多科学计算包在 3.12 上兼容最稳。命令如下（阶段0 时我们会一步步带你执行并验证）：

   ```bash
   conda create -n py312 python=3.12 -y
   conda activate py312
   python -m pip install --upgrade pip
   ```

2. **常用 conda 命令速查**（后面会反复用到）

   ```bash
   conda env list            # 看有哪些环境
   conda activate py312      # 激活
   conda deactivate          # 退出
   conda env remove -n xxx   # 删除环境
   conda list -n py312       # 查看已装包
   ```

3. **密钥与敏感信息管理——为什么重要**
   API key 是"钱 + 身份凭证"。绝不写进代码、绝不提交到 Git。
   习惯：把 key 放进环境变量或 `.env` 文件（且 `.env` 加入 `.gitignore`）。

   ```bash
   # ~/.bashrc 追加一行后 source ~/.bashrc
   export DEEPSEEK_API_KEY="sk-..."
   ```

4. **git 与 GitHub 工作流补全**
   你已有基础了解，缺两块：SSH key 生成并添加到 GitHub（免密推送）、`.gitignore`（忽略虚拟环境/密钥/大文件）。阶段0 会带你完成，并把 `ai-learning` 初始化为一个仓库。

5. **包安装源**：境外源慢时，DeepSeek 所在的国内网络通常可走清华/阿里镜像。到用时再配。

### 2.3 成本与算力策略（尽量低成本）

| 事项 | 成本 | 说明 |
|---|---|---|
| 本地 CPU 跑小模型 / 小实验 | 0 元 | 阶段2、3 主战场 |
| DeepSeek API | 几乎可忽略（按 token 计费，用量小基本几元内） | 用于阶段1、以及作为你"随时可问的老师" |
| Google Colab 免费 GPU（T4） | 0 元 | 阶段4 微调/中大型训练用，有会话时长限制 |
| Kaggle Notebook 免费 GPU | 0 元 | Colab 的备选 |
| 开源模型下载（HuggingFace / ModelScope） | 0 元 | 国内可用 ModelScope 镜像提速 |

> 约束：是否真的能连通 Colab/HuggingFace 取决于你的网络，见 TBD-3。若不通，阶段4 可全部走"本地 CPU 小模型"路线，只是规模更小。

### 2.4 资料与检索习惯

- 中文优先：PyTorch 官方中文文档、sklearn 文档、各大模型中文博客；
- 官方文档 > 二手教程 > 视频；英文原版文档看不懂再找中文；
- 把"查到的、验证过的结论"写进你自己的笔记（产出式学习，后面每个 lab 都要写小结）。

---

## 3. 需要你先确定/选择的事项（TBD 清单）

以下事项先留空，不阻塞开始。等我们开始前或进行到对应阶段时逐项确认。

| 编号 | 待定问题 | 备选方向 | 建议默认（若你不确定） |
|---|---|---|---|
| TBD-1 | 阶段1 先做哪些"AI 生产应用"方向 | AI 编程助手 / AI 对话问答 / AI 视频 / AI 画图 / AI 文档与知识库 / Agent与skill·MCP / 其他 | ~~待定~~ → **已定（2026-09-08）：五方向全做，顺序见 4.4** |
| TBD-2 | DeepSeek API key：是否已有？是否现在申请？key 的存放方式 | 环境变量 / .env | 没有则先去申请，放进 `~/.bashrc` 环境变量 |
| TBD-3 | 网络能否访问 Google Colab / HuggingFace | 能 / 不能 / 未测试 | 阶段4 前实测再定；不能则走纯本地小模型线 |
| TBD-4 | 阶段4 "你自己的模型"更想做哪种 | 微调小 LLM / 从0训迷你 GPT / 图像分类微调 / 其它领域 | 到时根据前几阶段表现一起定 |
| TBD-5 | 学习材料语言偏好 | 中文 / 中英结合 | 中文为主、关键概念对照英文原文 |
| TBD-6 | 是否先用几天补 Python 数据三件套（numpy/pandas/matplotlib）+ Jupyter | 补 / 边学边补 | **边用边补**，不必单独占用整块时间 |
| TBD-7 | `ai-learning` 仓库设为公开还是私有 | 公开 / 私有 | 公开（作为作品展示），介意再改私有 |
| TBD-8 | 你的 GitHub 账号名（用于建仓库提示） | — | 告诉我即可 |

> ~~以下两项建议现在不必回答（已于 2026-09-08 确认）：TBD-1 五方向全做；TBD-2 key 已有。~~ 请到第 4.4 节看执行顺序。

---

## 4. 阶段1：AI 工具在生产中的实际应用

### 4.1 思路

每个方向不做"泛泛体验"，而是**带着一个真实、可交付的小任务**做透，产出放进 `~/ai-learning/tools/<方向>/`。每个方向至少包含：README（做了什么、怎么复现）、使用的提示词/脚本、运行结果记录。

### 4.2 候选方向一览（挑你感兴趣的展开）

| 方向 | 典型产出 | 难度/成本 | 顺带学到 |
|---|---|---|---|
| AI 编程助手 | 用 AI 帮你完成一个真实小脚本/网页并入库 | 低 | 提示词工程、代码审查、git 协作 |
| AI 对话/问答机器人 | 一个基于 DeepSeek 的终端/网页问答助手 | 低 | API 调用、上下文管理、结构化输出 |
| AI 画图 | 用提示词产出成套素材（头像/海报图） | 低 | 提示词技巧、风格一致性 |
| AI 视频 | 文生视频 / 剪辑脚本自动化 | 中 | 工作流编排、多工具协作 |
| AI 文档/知识库 | 给一批文档做自动问答（RAG 雏形） | 中高 | 检索、向量、为阶段3 铺路 |

### 4.3 步骤拆解示范（以"AI 编程助手"为例，展示方法论）

> 说明：这只是一个模板示范。真正执行时，每个小项目我都会按"先讲这步为什么、再给实现、最后验证"来带你。

1. **Step 1 明确任务**：选定一个"不用 AI 也能做、但用 AI 更快"的小脚本（如：把某个文件夹里的图片按拍摄日期批量重命名整理）。——为什么：任务要小到当天能完成、又能暴露 AI 的边界。
2. **Step 2 用自然语言让 AI 写初稿**：你写需求 → AI 出代码 → 存到 `tools/ai-coding/`。
3. **Step 3 你在本地跑起来**：安装依赖、运行、观察报错——这一步逼你看懂每行在干嘛，而不是"复制粘贴就算完"。
4. **Step 4 带着报错反向追问 AI**：学会"把错误信息和期望行为描述清楚"——这是提示词工程最实用的能力。
5. **Step 5 审查与重构**：让 AI 解释这段代码、指出潜在 bug，你决定接受/拒绝——建立"AI 是协作者不是代笔"的心态。
6. **Step 6 沉淀**：写 README 记录需求→过程→成品→踩坑，提交到 git。**这步最重要**，它把"用过"变成"学过"。

> 其它方向进入时按同一模板展开。执行中每一步我都会分步带你。

### 4.4 执行顺序（五个方向全做，按依赖递进）

| 顺序 | 方向 | 为什么排这里 |
|---|---|---|
| ① | AI 对话/问答机器人（含 MCP 工具调用） | 地基：先学会 API 调用、上下文管理、结构化输出、function calling / MCP 工具调用这些"最小可复用能力" |
| ② | Agent 开发 + skill/MCP（主线） | 建立在①的工具调用之上：理解 agent 如何自动规划→调用工具→完成任务；再学自定义 skill、配置/开发 MCP server |
| ③ | AI 文档/知识库问答（RAG 雏形） | 引入检索与向量，衔接阶段3 的 embedding/Transformer |
| ④ | AI 画图 | 与前三者相对独立，主要练提示词与素材生产工作流，可插空并行 |
| ⑤ | AI 视频 | 工作流最重、依赖最多，放最后且只需做到"跑通一个最小样片"即可 |

> 注意：①②③中都会用到你实际的工具环境（opencode + DeepSeek），每个方向产出放进 `~/ai-learning/tools/<方向>/`。

---

## 5. 阶段2：机器学习（原理 + 实践）

### 5.1 知识主干与学习顺序

把"数学基础"重新落到"能解释现象"的位置，而不是重新学一遍数学：

1. **热身**：numpy 数组操作、matplotlib 画图、pandas 表格（边做边学）；运行环境用 Jupyter 便于边写边看。
2. **从 0 实现线性回归**：一个变量 → 用梯度下降手工迭代调参。这一步打通"损失函数 + 梯度 + 学习率"的直觉，是全书最关键的第一课。
3. **用 sklearn 对照**：同样的数据用现成库做，对比你手写的结果——建立"手写为了懂、库为了用"的平衡。
4. **分类问题**：逻辑回归、交叉熵损失；二分类到多分类。
5. **评估的科学**：训练集/验证集/测试集为什么分开、过拟合长什么样、正则化（L1/L2）、交叉验证。**这是最容易让新手"自我感觉良好但实际无效"的地方，会重点实践。**
6. **经典模型巡礼**：K 近邻、决策树、随机森林、朴素贝叶斯；配合 sklearn 自带小数据集（鸢尾花、手写数字、房价）做 2~3 个完整小项目。
7. **特征工程与流水线（pipeline）**入门：数据清洗、标准化、类别编码、用 pipeline 串起来。

> 每学一个算法：先在纸上/白板上推演它做了什么 → 用 numpy 从0写一遍 → 用 sklearn 对照 → 写小结笔记到 `~/ai-learning/ml/notes/`。

### 5.2 阶段产出

- 每个核心算法从0实现的代码（可运行、有对比结论）；
- 2~3 个完整小项目（含评估报告）；
- 一套你自己的"实验记录"习惯（每跑一次实验记下参数与结果）。

### 5.3 参考（选定后给具体章节）

- 中文书籍/视频推荐待定（TBD-5 确定后给）。官方文档：sklearn、numpy。

---

## 6. 阶段3：深度学习（原理 + 实践）

### 6.1 为什么选 PyTorch

生态最活跃、学术界与工业界主流、文档好、和"微调开源模型"（阶段4）工具链最顺。PyTorch 与是否有 GPU 无关，CPU 上完全可以学。

### 6.2 知识主干与学习顺序

1. **张量（tensor）与自动求导（autograd）**：把阶段2 的"手工算梯度"交给框架自动完成——理解它是怎么自动做到的。
2. **用 numpy 从0写一个 2 层神经网络**并手推反向传播一次：这是从"机器学习"跨到"深度学习"最关键的一跃，做完后你会发现前面学的"梯度下降"在这里只是"链式法则应用"。
3. **用 PyTorch 重写上面那个网络**：体会框架替你做了什么、没替你做什么。
4. **MLP 与训练技巧**：学习率、batch size、权重初始化、激活函数、观察 loss 曲线判断欠拟合/过拟合。
5. **CNN（卷积网络）**：图像任务；用 MNIST/CIFAR 小数据集 + 小网络，CPU 可跑。
6. **embedding 与序列**：词向量、RNN 看一眼即可，重点理解"序列数据怎么进网络"。
7. **注意力机制与 Transformer 结构**：这是打通 LLM 的钥匙。用可视化材料 + 手动拆解注意力计算，把概念和图景连起来。
8. **最小 LLM 实验**：以 nanoGPT 风格的小型 char-level GPT，在你的 CPU 上从0训练几十万 token 的极小模型，观察它"学会造句"——**这一步会让"训练原理"彻底落地**，是阶段4 的桥。

### 6.3 阶段产出

- 从0实现（numpy）的神经网络代码；
- PyTorch 版的若干小实验（含 loss 曲线、评估）；
- 一个能跑通的最小 LLM 训练实验及"它学到了什么"的分析笔记。

### 6.4 参考

- PyTorch 官方教程（中文）；"动手学深度学习"（中文，免费开源）等，选定后给具体章节。

---

## 7. 阶段4：训练 / 微调你自己的模型（最终产出）

按你的兴趣 + 算力条件，从下面产出一条线（可组合）：

| 产出线 | 做什么 | 算力需求 | 说明 |
|---|---|---|---|
| A. 微调开源小 LLM | 用 LoRA 在自己的文本数据上微调一个小开源模型（如 Qwen 小尺寸），做出"能对话/能处理你的领域文本"的 demo | 需免费云 GPU（TBD-3） | 最贴近"微调"一词，交付一个真能用的模型 |
| B. 从0训一个迷你模型 | 在第 6.2-8 的小 LLM 基础上放大/改进，或用自己数据集从0训一个分类/生成小模型 | 本地 CPU 即可 | 最硬核，理解最深 |
| C. 视觉/其它迁移学习 | 用预训练模型在你自己的小数据集上微调（如图片分类） | 本地 CPU / 小模型可跑 | 门槛较低、见效快 |

> 无本地 GPU 的对策：优先选尺寸适配 CPU 或免费 Colab/Kaggle 能跑的模型；训练完成后用 GGUF 量化，甚至可以在你自己的 CPU 笔记本上跑推理——这是"能自己训 + 能在自己机器上用"的最小闭环。

**交付标准（缺一不可）**：能讲清楚模型每个主要部分为什么这样设计；训练过程可复现（记录数据、超参数、随机种子）；有评估结果（不是"看起来像"）；推理 demo 可运行。

---

## 8. 学习方法论（贯穿全程，最重要）

1. **逐步拆解，不一次给全答案**：每个实践任务拆成若干步；每步先讲"为什么做这步"，再给"怎么做"与"具体实现"，最后"怎么验证这一步成了"。我不会把一整段工程一次性丢给你。
2. **先跑通，再深究**：第一遍目标是把最小路径跑绿；跑通后再回到代码里逐行问"为什么"，此时问题才问得准。
3. **用自己的话写小结**：每个 lab 结束写 3~5 行小结（我学到了什么/哪里还不懂）。"输出"是最好的学习放大器。
4. **实验要留痕**：记录数据来源、参数、结果、日期。AI 学习本质是"经验科学"，不记录等于白做。
5. **善用 AI 但先自己思考**：DeepSeek 是你的老师/搭档。提问原则：先给出你自己的理解，再请它纠正/补充——而不是让它替你想。
6. **允许回头看**：数学/概念卡住时，回到对应的小实验，用"改数字看变化"的方式重建直觉，而不是硬啃公式。

---

## 9. 目录结构与 Git 建议

目标目录树：

```
~/ai-learning/
├── LEARNING_ROADMAP.md   # 本文件
├── README.md             # 项目总览（对外展示）
├── tools/                # 阶段1：AI 工具应用，每个方向一个子目录
│   ├── ai-coding/
│   └── ...（按你选的方向）
├── ml/                   # 阶段2：机器学习
│   ├── labs/             # 从0实现 + 对照实验
│   ├── projects/         # 完整小项目
│   └── notes/            # 学习笔记
├── dl/                   # 阶段3：深度学习（同上结构）
└── capstone/             # 阶段4：最终模型产出
```

Git 建议：
- `~/ai-learning` 初始化为 git 仓库（阶段0 第 1 步完成），按阶段建分支或直接主分支推进皆可；
- `.gitignore` 必含：密钥文件、conda 相关、数据缓存、大型模型权重文件（权重可用 ModelScope/HF 链接代替上传）；
- 提交信息用"做了什么事"的一句话，保持小步提交。

---

## 10. 下一步行动

1. ✅ 已定（2026-09-08）：阶段1 五方向全做，顺序见 4.4；DeepSeek key 已有。
2. 我们从 **阶段0 第 1 步**开始：新建 `py312` 环境并装好基础包（含 Jupyter、numpy/pandas/matplotlib）；
3. 阶段0 完成前，我会顺带带你完成：git 仓库初始化 + SSH key + GitHub 关联、`.gitignore`、`DEEPSEEK_API_KEY` 环境变量——之后阶段1 即可随时开工。
4. 阶段1 第 1 个小项目按 4.4 顺序执行：**AI 对话/问答机器人（含 MCP 工具调用）**。

> 本文件会随进度持续更新（每完成一段就回填实际结果与调整）。建议每次会话开始时先读一遍"当前进度"定位到下一步。
